{
  "name": "skeleton-stylus",
  "version": "2.0.4",
  "description": "Skeleton: A Dead Simple, Responsive Boilerplate for Mobile-Friendly Development",
  "scripts": {
    "build": "stylus -o dist skeleton.styl",
    "watch": "stylus -o dist -w skeleton.styl"
  },
  "devDependencies": {
    "stylus": "^0.54.5"
  }
}
